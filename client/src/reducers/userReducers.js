import {

} from "../constants/userConstants";

const initialState = {
   
  };

export default function usuario(state = initialState, action) {
    switch (action.type) {
        default:
      return state;
    }
}